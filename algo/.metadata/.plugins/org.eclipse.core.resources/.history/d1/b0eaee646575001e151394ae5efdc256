package BOJ_1477_휴게소세우기;

import java.util.Scanner;

public class Main {
	static int N, M, L;
	
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		// N : 현재 휴게소의 갯수
		// M : 추가로 지으려는 휴게소의 갯수
		// L : 고속도로의 길이
		N = sc.nextInt();
		M = sc.nextInt();
		L = sc.nextInt();
		// 휴게소의 위치
		int[] restPlace = new int[N];
		for(int i = 0; i < N; i++) restPlace[i] = sc.nextInt();
		// 휴게소 위치 입력 끝
		
		int start = 0;
		int end = L;
		
		while(start+1 < end) {
			int mid = (start+end)/2;
			
			if () {
				
			}
			
			
		}
		
	}
}
